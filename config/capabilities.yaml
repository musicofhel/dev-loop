# Per-project capability scoping (TB-1: CLAUDE.md rules, not OpenFang)
# See docs/layers/03-runtime.md for details

OOTestProject1:
  allowed_tools:
    - Read
    - Write
    - Edit
    - Glob
    - Grep
    - Bash  # scoped to test/lint commands only
  denied_paths:
    - .env
    - secrets/
    - "*.pem"
    - "*.key"
  bash_allowlist:
    - "uv run pytest"
    - "uv run python"
    - "git status"
    - "git diff"
    - "git add"
    - "git commit"
