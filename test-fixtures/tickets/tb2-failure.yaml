# TB-2 test ticket — designed to trip Gate 0 on first attempt
# The pre-seeded test at test-fixtures/tests/test_factorial_trap.py
# includes edge cases (float input, negative input) that agents
# commonly miss on the first try.

id: MOCK-TB2
title: "Add factorial function with edge case handling"
repo: OOTestProject1
labels: [feature]
priority: high
description: |
  Add a `factorial` function to `src/oo_test_project/calculator.py`.

  Requirements:
  - factorial(0) == 1
  - factorial(5) == 120
  - factorial(-1) raises ValueError("Factorial not defined for negative numbers")
  - factorial(1.5) raises TypeError("Factorial requires integer input")

  Tests already exist in tests/test_factorial.py — make them pass.
status: Ready
